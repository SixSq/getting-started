[
    {basicAuth, []},

    % HNSciCloud RHEA Federated Identity Provider based on Keycloak.
    {rhea, [
            % Standard config
            {auth_module, auth_keycloak},
            {app_id, <<"">>},
            {app_secret, <<"">>},
            % Provider specific config
            {xrds_endpoint, <<"/.well-known/openid-configuration">>}
        ]}
].
